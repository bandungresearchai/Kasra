# Copy this file to .env.local and fill the values.
#
#   cp .env.example .env.local
#

# Coinbase Developer Platform (CDP) credentials
CDP_API_KEY_NAME=
CDP_API_KEY_PRIVATE_KEY=

# Optional: defaults to "base-sepolia"
NETWORK_ID=base-sepolia

# OpenAI
OPENAI_API_KEY=

# Optional: defaults to "gpt-4o-mini"
OPENAI_MODEL=gpt-4o-mini

# OnchainKit (frontend)
# Provide either NEXT_PUBLIC_ONCHAINKIT_API_KEY (to use CDP RPC) OR NEXT_PUBLIC_RPC_URL
NEXT_PUBLIC_ONCHAINKIT_API_KEY=
NEXT_PUBLIC_RPC_URL=

# Demo transaction config (frontend)
# IDRX token contract address on the selected chain (e.g., Base Sepolia)
NEXT_PUBLIC_IDRX_ADDRESS=
# Optional: fallback recipient address if KASRA's "Ke:" is not a 0x address
NEXT_PUBLIC_DEMO_RECIPIENT_ADDRESS=
